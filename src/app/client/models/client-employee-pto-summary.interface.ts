export interface IClientEmployeePTOSummary {
  referenceNumber: string;
  effectiveDate: string;
  absenceCode: string;
  absenceDescription: string;
  hoursAbsent: string;
  hoursAccrued: string;
  hoursCarriedOver: string;
  comment: string;
}
